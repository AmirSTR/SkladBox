{
  "name": "antistop-mvp",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client && npm install --prefix server && npm run build --prefix server && npx prisma generate --schema=server/prisma/schema.prisma",
    "start": "node server/dist/index.js",
    "postinstall": "npm install --prefix client && npm install --prefix server",
    "migrate:deploy": "npx prisma migrate deploy --schema=server/prisma/schema.prisma"
  },
  "devDependencies": {
    "prisma": "^6.1.0"
  }
}
